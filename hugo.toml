baseURL = "https://mapgrids.olaru.dk/"
title = "Map Grids"

[languages.en]
  languageCode = "en-us"
  weight = 1

disableKinds = ["taxonomy", "term", "RSS", "sitemap"]

[params]
  description = "Printable isometric & dimetric grid generator"

[markup.goldmark.renderer]
  unsafe = true
